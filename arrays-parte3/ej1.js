let numeros = [1, 2, 3, 4, 5, 6];
[a, b, ...resto] = numeros;
console.log(a);
console.log(b);
console.log(resto);


/**Crea un array llamado numeros con al menos seis números.
 *  Usa desestructuración para obtener los primeros dos 
 * números en variables individuales y 
 * almacena el resto de los números en un array llamado resto */