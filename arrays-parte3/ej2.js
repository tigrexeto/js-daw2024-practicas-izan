let valores = [11, 22, 33, 11, 22, 44];
/* let lista = new Set(valores); */
let valoresUnicos = [...new Set(valores)];
console.log(valoresUnicos);

/**Crea un array llamado valores que contenga números, algunos de los cuales 
 * son duplicados. Utiliza un Set 
 * para crear un nuevo array que contenga solo los números únicos y luego imprímelo. */